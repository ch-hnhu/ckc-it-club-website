# chạy lại migration và seed dữ liệu
php artisan migrate:fresh --seed

# tạo symbolic link để truy cập file trong thư mục storage
php artisan storage:link

# chạy lại swagger
php artisan l5-swagger:generate
