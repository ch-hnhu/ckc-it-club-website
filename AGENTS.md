# CKC IT Club Monorepo Agent Entry

- Canonical project context directory: `.agents/context/`
- Start with `.agents/context/README.md`.
- Then route by app:
- `backend/` -> `.agents/context/backend.md`
- `frontend/admin/` -> `.agents/context/frontend-admin.md`
- `frontend/user/` -> `.agents/context/frontend-user.md`
- This file is intentionally minimal so the real context stays centralized under `.agents/`.
